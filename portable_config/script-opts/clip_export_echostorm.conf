# Where exported clips are saved.
save_location=~~desktop/mpv/clips/

# Name/path of the ffmpeg binary. Left as just the exe name since the
# installer places ffmpeg.exe in the same install root as mpv.exe, which
# Windows already searches first for a bare exe name.
ffmpeg_path=ffmpeg.exe
