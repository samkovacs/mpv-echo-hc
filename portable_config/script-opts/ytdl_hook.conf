# Pin the resolver to yt-dlp explicitly. The installer downloads yt-dlp.exe
# (not youtube-dl), and this guarantees ytdl_hook uses it regardless of
# what a given mpv build defaults to or what's on PATH.
ytdl_path=yt-dlp
